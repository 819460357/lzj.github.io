/**
 * mysql配置
 * @type {{connectionLimit: number, host: string, user: string, password: string, database: string}}
 */
exports.mysql = {
    connectionLimit : 100,
    host            :'127.0.0.1',
    user            : 'root',
    password        : '123456',
    database        : 'qrcode'
}
